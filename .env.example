# Local-only Worker maintenance authorization. Generate a fresh random value.
INTERNAL_SECRET=

# Optional future session signing boundary; v0.1 sessions are opaque random tokens.
SESSION_SIGNING_SECRET=

# Deployment values are intentionally unset and never used by local verification.
CLOUDFLARE_API_TOKEN=
CLOUDFLARE_ACCOUNT_ID=
CLOUDFLARE_D1_DATABASE_ID=
